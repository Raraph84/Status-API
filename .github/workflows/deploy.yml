name: Auto Déploiement
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - deploy: executing remote ssh commands using password
      uses: appleboy/ssh-action@master
      with:
        host: ssh-raraph84.alwaysdata.net
        username: raraph84_status_deploy
        password: ${{ secrets.SSH_PASSWORD }}
        script: ~/deployApi.sh
